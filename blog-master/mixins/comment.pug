mixin comment(comment)
    include replyform
    div.comment
        div.comment-header
            div.comment-author=comment.author_name
            div.comment-time=comment.commentedAgo
        div.comment-body=comment.body
        div
            h4.reply-header Replies (#{comment}

